# Environment variables / secrets
.env
.env.*
!.env.example
secrets/*
!secrets/ci.yaml
*.json.key
firebase-service-account*.json
google-services.json
GoogleService-Info.plist

# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
*.g.dart
coverage/

# gen-l10n の生成物（l10n.yaml参照）
lib/l10n/gen/

# Node.js（npx supabase / firebase-tools 用。package.json/package-lock.jsonはCLIバージョン
# 固定のためコミット対象、node_modules/のみ除外）
node_modules/

# Supabase local
# supabase/.gitignore で .branches / .temp 等は別途管理済み

# IDE
.idea/
.vscode/
*.iml

# Store listing screenshot pipeline: raw device captures are ephemeral local
# output of capture_screenshots.js, regenerated on demand (see assets/store/listing/README.md).
assets/store/listing/raw/
