name: Create GitHub Release Draft

on:
  push:
    branches:
      - production

permissions:
  contents: write

jobs:
  create-draft:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Determine next version (v0.0.N, auto-increment from existing releases)
        id: version
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          LATEST=$(gh release list -R "${{ github.repository }}" --limit 200 --json tagName -q '.[].tagName' \
            | grep -E '^v0\.0\.[0-9]+$' \
            | sed -E 's/^v0\.0\.//' \
            | sort -n | tail -1)
          NEXT=$(( ${LATEST:-0} + 1 ))
          echo "version_code=$NEXT" >> "$GITHUB_OUTPUT"
          echo "tag=v0.0.$NEXT" >> "$GITHUB_OUTPUT"

      - name: Create release draft with auto-generated notes
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${{ steps.version.outputs.tag }}" \
            --draft \
            --generate-notes \
            --target "${{ github.sha }}" \
            --title "${{ steps.version.outputs.tag }}"

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install sops / yq
        run: |
          curl -sL -o /usr/local/bin/sops https://github.com/getsops/sops/releases/download/v3.9.4/sops-v3.9.4.linux.amd64
          chmod +x /usr/local/bin/sops
          curl -sL -o /usr/local/bin/yq https://github.com/mikefarah/yq/releases/download/v4.44.3/yq_linux_amd64
          chmod +x /usr/local/bin/yq

      - name: Decrypt secrets (SOPS)
        env:
          SOPS_AGE_KEY: ${{ secrets.SOPS_AGE_KEY }}
        run: |
          sops --decrypt secrets/ci.enc.yaml > "$RUNNER_TEMP/ci-secrets.yaml"

      - name: Write .env
        run: |
          {
            echo "SUPABASE_URL=$(yq -r '.SUPABASE_URL' "$RUNNER_TEMP/ci-secrets.yaml")"
            echo "SUPABASE_ANON_KEY=$(yq -r '.SUPABASE_ANON_KEY' "$RUNNER_TEMP/ci-secrets.yaml")"
            echo "GOOGLE_OAUTH_CLIENT_ID=$(yq -r '.GOOGLE_OAUTH_CLIENT_ID' "$RUNNER_TEMP/ci-secrets.yaml")"
            echo "FLUTTER_APP_ENV=prod"
          } > .env

      - name: Restore google-services.json
        run: yq -r '.GOOGLE_SERVICES_JSON' "$RUNNER_TEMP/ci-secrets.yaml" > android/app/google-services.json

      - name: Restore upload keystore
        run: yq -r '.ANDROID_KEYSTORE_BASE64' "$RUNNER_TEMP/ci-secrets.yaml" | base64 -d > "$RUNNER_TEMP/upload-keystore.jks"

      - name: Build signed App Bundle
        env:
          ANDROID_KEYSTORE_PATH: ${{ runner.temp }}/upload-keystore.jks
        run: |
          ANDROID_KEYSTORE_PASSWORD="$(yq -r '.ANDROID_KEYSTORE_PASSWORD' "$RUNNER_TEMP/ci-secrets.yaml")"
          ANDROID_KEY_ALIAS="$(yq -r '.ANDROID_KEY_ALIAS' "$RUNNER_TEMP/ci-secrets.yaml")"
          ANDROID_KEY_PASSWORD="$(yq -r '.ANDROID_KEY_PASSWORD' "$RUNNER_TEMP/ci-secrets.yaml")"
          export ANDROID_KEYSTORE_PASSWORD ANDROID_KEY_ALIAS ANDROID_KEY_PASSWORD
          flutter pub get
          flutter build appbundle --release \
            --build-name="0.0.${{ steps.version.outputs.version_code }}" \
            --build-number="${{ steps.version.outputs.version_code }}"

      - name: Remove decrypted secrets
        if: always()
        run: rm -f "$RUNNER_TEMP/ci-secrets.yaml" "$RUNNER_TEMP/upload-keystore.jks"

      - name: Attach App Bundle to draft Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release upload "${{ steps.version.outputs.tag }}" build/app/outputs/bundle/release/app-release.aab --clobber
